# Production .env template for the shared-hosting release.
#
# On the live host, copy this content into a file named `.env` (via cPanel
# File Manager or FTP) and fill in the placeholders below. Never commit the
# filled-in version anywhere — this template only ever holds placeholders.
#
# Generate APP_KEY locally with: php artisan key:generate --show
# (it just prints a value — running it doesn't touch your local .env)

APP_NAME="STC Scouts"
APP_ENV=production
APP_KEY=                           # REPLACE — see note above
APP_DEBUG=false
APP_URL=https://www.stcscouts.com  # REPLACE if the real production domain differs

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=sqlite

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

# Shared password for the /admin report manager.
# REPLACE — pick a strong password, not the dev default (2606password).
ADMIN_PASSWORD=
